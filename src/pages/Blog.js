import React from 'react';
import styled from 'styled-components';

import BlogContainer from '../views/blog/containers/list';

const Blog = () => (
  <Container>
    <BlogContainer />
  </Container>
);

const Container = styled.div`

`;

export default Blog;
